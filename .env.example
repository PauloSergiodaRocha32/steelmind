# Gestio ERP integration (server-side only — never commit real credentials)
GESTIO_EMAIL=
GESTIO_PASSWORD=
GESTIO_API_BASE_URL=https://api.otkweb.com.br

# Auth (required in production for local JWT mode)
AUTH_SECRET=

# Supabase (optional — falls back to data/steelmind/store.json + local users)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
